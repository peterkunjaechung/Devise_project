<%= button_to "Log Out", destroy_user_session_path, class: "btn red", method: :delete %>
<h2>Welcome <%= current_user.first_name %></h2>
<hr />
<%= link_to "Add Expenses", new_account_path %>
<h3>Your Expenses</h3>
<ul>
  <% @accounts.each do |a| %>
    <li>
      <%= a.name %> - $<%= a.balance %>
    </li>
  <% end %>
</ul>
